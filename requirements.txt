# PyTorch는 별도 설치 (Blackwell = CUDA 12.8):
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# 아래는 그 다음 설치.

# 학습 스택
transformers>=4.48.0
datasets>=3.2.0
accelerate>=1.2.0
peft>=0.14.0
trl>=0.13.0
bitsandbytes>=0.45.0        # Blackwell(sm_120) 지원은 최신 버전 필요
# Windows + Blackwell 무컴파일 검증 경로는 SDPA attention을 사용한다.
# unsloth/flash-attn은 환경별 소스빌드 이슈가 있어 기본 의존성에서 제외.

# 모델 병합 (SOLAR식 up-scaling / SLERP·DARE·TIES)
mergekit

# 평가 — 한국어 벤치마크 (LogicKor/KMMLU/HAERAE 등)
lm-eval>=0.4.0

# 유틸
pyyaml>=6.0
python-dotenv>=1.0
huggingface_hub>=0.27
wandb                        # 선택: 실험 추적 (report_to로 켜기)
